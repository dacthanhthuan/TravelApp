import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    container:{
        flexDirection:'row',
        alignItems: 'center'
    },
    title: {
        fontSize: 12,
        color: '#000',
        marginHorizontal: 8
    },
    icon:{
        width: 40,
        height: 40,
    }
});

export default styles;